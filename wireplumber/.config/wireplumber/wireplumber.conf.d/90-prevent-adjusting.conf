access.rules = [
  {
    matches = [
      {
        application.process.binary = "Chromium input"
      }
    ]
    actions = {
      update-props = {
        default_permissions = "rx"
      }
    }
  }
]

