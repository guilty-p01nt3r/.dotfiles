#!/bin/bash


#pkill -9 -f '.*\.exe$'

while true; do
    sleep 1
    pid=$(pgrep -f '.*\.exe$')
    if [ -z "$pid" ]; then
        echo "No process found"
        break
    fi
    if [ -n "$pid" ]; then
        kill -9 $pid
    fi
done
