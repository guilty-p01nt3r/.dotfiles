# Sway env_variables for wlroots support 
#exec --no-startup-id systemctl --user import-environment DISPLAY SWAYSOCK WAYLAND_DISPLAY  XDG_CURRENT_DESKTOP
#exec --no-startup-id dbus-update-activation-environment --systemd DISPLAY SWAYSOCK WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
#exec --no-startup-id ~/.dotfiles/scripts/sway/repairportal
#exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway


# gnome keyring
#exec dbus-update-activation-environment --all
exec gnome-keyring-daemon --start --components=secrets

# Mod1 = Alt | Mod4 = WinKey
set $mod Mod1
#set $mod 65513

# Fonts
font pango:Ubuntu Mono Nerd Font 11

# Network Manager Applet
exec --no-startup-id nm-applet --indicator

# Screenshot software 
#exec --nostartup-id flameshot &;
#bindsym Print exec flameshot gui --raw | wl-copy;
bindsym Print exec /usr/bin/grimshot copy area;
bindsym Shift+Print exec /usr/bin/grimshot save area;
bindsym Ctrl+Print exec grim -g "$(slurp)" - | swappy -f -

# Color picker
bindsym Ctrl+Shift+Print exec hyprpicker -a 


# KdeConnect (Mobile Phone Integration)
exec --no-startup-id kdeconnect-indicator &

# Easy Effects
exec --no-startup-id flatpak run com.github.wwmm.easyeffects --gapplication-service

# Bar 
bar {
    swaybar_command waybar
    position top
    mode dock
}

bindsym Mod4+minus bar mode toggle


# change volume or toggle mute | TODO: use waybar
### Using pamixer 
bindsym $mod+Shift+x exec --no-startup-id ~/.config/sway/audioManager.sh sink-default-increase
bindsym $mod+Shift+z exec --no-startup-id ~/.config/sway/audioManager.sh sink-default-decrease
bindsym $mod+Shift+b exec --no-startup-id ~/.config/sway/audioManager.sh sink-default-tmute
## Mic toggle
bindsym $mod+Shift+v exec --no-startup-id ~/.config/sway/audioManager.sh source-default-tmute

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
set $term kitty
bindsym $mod+Return exec --no-startup-id $term

#start calculator(Python)
bindsym $mod+c exec --no-startup-id $term --class="pycalc" -e python3 -i -c 'import math'
#start calculator (bc)
#bindsym $mod+c exec --no-startup-id $term --class="calc" -e bc -q -l

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#rofi 
set $drun rofi -show drun -show-icons -config ~/.config/rofi/config 
#bindsym $mod+Shift+d exec --no-startup-id rofi -show window -show-icons -config ~/.config/rofi/config 

#wofi
#bindsym $mod+d exec --no-startup-id wofi --show drun --allow-images 
#bindsym $mod+Shift+d exec --no-startup-id wofi --show window  --allow-images 

#tofi
# Launch directly
#set $drun sed --in-place "s/^output =.*/output = $(swaymsg --type get_outputs | jq '.[] | select(.focused).name')/" ~/.config/tofi/config && tofi-drun --drun-launch=true --font "~/.local/share/fonts/Fira Code Regular Nerd Font Complete Mono.ttf"
bindsym $mod+d exec --no-startup-id $drun

#exec_always --no-startup-id clipcatd # start clipcatd at startup
#set $launcher-clipboard-insert clipcat-menu insert
#set $launcher-clipboard-remove clipcat-menu remove
#bindsym $mod+p exec $launcher-clipboard-insert
#bindsym $mod+o exec $launcher-clipboard-remove

## Movement ## 
# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+o split v

# split in vertical orientation
bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
set $ws14 "14"
set $ws15 "15"
set $ws16 "16"
set $ws17 "17"
set $ws18 "18"
set $ws19 "19"
set $ws20 "20"
set $vws1 "v1"

# Define names for monitors
set $mon1 DP-1
set $mon2 DP-3
set $mon3 HDMI-A-1
#set $vmon1 HDMI-A-1
set $vmon1 HEADLESS-1

# Monitors config 
#output * adaptive_sync off
#output $mon1 adaptive_sync on

output $mon1 {
    mode 1920x1080@144Hz
    #mode 1920x1080@99Hz
    #mode 1920x1080@60Hz
    position 0,0 
    #render_bit_depth 8
    adaptive_sync off
    allow_tearing on
    max_render_time off
}
# 1920x1080 119.88 Hz (CVT) hsync: 137.14 kHz; pclk: 285.25 MHz
#output $mon1 modeline 285.25 1920 1968 2000 2080  1080 1083 1088 1144 +hsync -vsync

# 1920x1080 59.93 Hz (CVT 2.07M9-R) hsync: 66.59 kHz; pclk: 138.50 MHz
# output $mon1 modeline  138.50  1920 1968 2000 2080  1080 1083 1088 1111 +hsync -vsync

output $mon2 {
    #mode 1920x1080@60Hz 
    position 1920,0
    #render_bit_depth 8
    allow_tearing on
    max_render_time off
    #adaptive_sync on
}
# 1920x1080 59.93 Hz (CVT 2.07M9-R) hsync: 66.59 kHz; pclk: 138.50 MHz
output $mon2 modeline  138.50  1920 1968 2000 2080  1080 1083 1088 1111 +hsync -vsync

#output $mon3 {
#  #mode 1920x1080@30Hz 
#  position 0,+1080
#  #render_bit_depth 8
#}

#output $mon3 modeline 65.76  1920 1928 1960 2000  1080 1082 1090 1096 +hsync -vsync


input type:pointer {
    accel_profile "flat"
    pointer_accel 0
}


#  AOC preferred mode
#output $mon1 modeline +hsync +vsync

# Samsung preferred mode
#output $mon2 modeline +hsync +vsync 


exec swaymsg "create_output $vmon1 resolution 1920x1080 disable"
output $vmon1 {
    pos 0,+1080
    mode 1920x1080@60Hz
    allow_tearing on
    max_render_time off
    adaptive_sync off
    disable
}

# HDMI preffered mode
#output $mon3 modeline 148.500 1920 2448 2492 2640 1080 1084 1089 1125 +hsync +vsync

# Lock Screen and DPMS
set $lock 'swaylock -f -c 212121'
set $bl_time 600

### Idle configuration
exec_always pgrep swayidle ||  swayidle -w \
    timeout $bl_time "swaymsg output $mon1 dpms off; swaymsg output $mon2 dpms off;"  \
    resume "swaymsg output $mon1 dpms on ; swaymsg output $mon2 dpms on ; " \
    before-sleep $lock


# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

bindsym $mod+F1 workspace $ws11
bindsym $mod+F2 workspace $ws12
bindsym $mod+F3 workspace $ws13
bindsym $mod+F4 workspace $ws14
bindsym $mod+F5 workspace $ws15
bindsym $mod+F6 workspace $ws16
bindsym $mod+F7 workspace $ws17
bindsym $mod+F8 workspace $ws18
bindsym $mod+F9 workspace $ws19
bindsym $mod+F10 workspace $ws20
bindsym $mod+F11 workspace $vws1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+F1 move container to workspace $ws11
bindsym $mod+Shift+F2 move container to workspace $ws12
bindsym $mod+Shift+F3 move container to workspace $ws13
bindsym $mod+Shift+F4 move container to workspace $ws14
bindsym $mod+Shift+F5 move container to workspace $ws15
bindsym $mod+Shift+F6 move container to workspace $ws16
bindsym $mod+Shift+F7 move container to workspace $ws17
bindsym $mod+Shift+F8 move container to workspace $ws18
bindsym $mod+Shift+F9 move container to workspace $ws19
bindsym $mod+Shift+F10 move container to workspace $ws20

# move workspace between monitor
bindsym $mod+Ctrl+1 move workspace to output $mon1
bindsym $mod+Ctrl+2 move workspace to output $mon2

workspace $ws1 output $mon1
workspace $ws2 output $mon1
workspace $ws3 output $mon1
workspace $ws4 output $mon1
workspace $ws5 output $mon1
workspace $ws6 output $mon2
workspace $ws7 output $mon2
workspace $ws8 output $mon2
workspace $ws9 output $mon2
workspace $ws10 output $mon2
workspace $ws11 output $mon1
workspace $ws12 output $mon1
workspace $ws13 output $mon1
workspace $ws14 output $mon1
workspace $ws15 output $mon1
workspace $ws16 output $mon1
workspace $ws17 output $mon1
workspace $ws18 output $mon1
workspace $ws19 output $mon1
workspace $ws20 output $mon3
workspace $vws1 output $vmon1

# Reset config
bindsym $mod+Shift+r exec "swaymsg reload"

# exit i3 (logs you out of your X session) TODO: sway way
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Wallpaper
#output $mon1 bg ~/Pictures/SpaceMonster.jpg fill
output $mon1 bg ~/Pictures/BatteryWallpaper.png fill
#output $mon2 bg $(shuf -n 1 -e ~/Pictures/brockHofer/WallpaperM2/*)  fill
output $mon2 bg ~/Pictures/getCapeWearCapeFly.jpg fill
output $mon3 bg #000000 solid_color

# Session Manager (gui)
#exec --no-startup-id /usr/bin/lxpolkit &

default_border pixel 1
default_floating_border pixel 1
for_window [class="^.*"] border pixel 2

smart_borders on

#gaps outer 10

# Notification
exec --no-startup-id mako
bindsym Ctrl+space exec makoctl dismiss
bindsym Ctrl+Shift+space exec makoctl dismiss -a

## application rules
assign [app_id="v1"] workspace v1

for_window [shell=".*"] title_format "%title :: %shell"

# Steam 
#for_window [class="^steam$"] floating enable
#for_window [title="^Steam Big Picture Mode$"] { 
#    focus
#    move to output $mon2
#    fullscreen enable
#}
#for_window [title="^Steam$"] { 
#    focus
#    move to output $mon2
#    fullscreen enable
#}
#for_window [class="^steam$"] { 
#    move to output $mon2
#}
#
for_window [class="gamescope"] {
    focus
    move to output $vmon1
    fullscreen enable
    border none
}
for_window [app_id="gamescope"] {
    focus
    move to output $vmon1
    fullscreen enable
    border none
}


for_window [class="^exiled-exchange-2$"] {
    no_focus
    floating enable
}

no_focus [class="^exiled-exchange-2$"]
for_window [class="exiled-exchange-2" instance="exiled-exchange-2"] floating enable
assign [class="steam_app_2694490" instance="steam_app_2694490"] 15
assign [class="exiled-exchange-2" instance="exiled-exchange-2"] 15
no_focus [class="exiled-exchange-2" instance="exiled-exchange-2"]
no_focus [tiling]



# Discord
assign [class="^discord$"] $ws10
for_window [class="^discord$"] fullscreen
# Teams
assign [class="^teams$"] $ws10
for_window [class="^teams$"] fullscreen

#for_window [app_id="lutris"] {
#    focus
#    move to output $vmon1
#}
for_window [app_id="heroic"] {
    focus
    move to output $vmon1
}
for_window [class="heroic"] {
    focus
    move to output $vmon1
}
#for_window [class="AppRun.wrapped"] {
#    focus
#    move to output $vmon1
#}
#for_window [app_id="rpcs3"] {
#    focus
#    move to output $vmon1
#    fullscreen
#
#}
for_window [app_id="duckstation-qt"] {
    focus
    move to output $vmon1

}
for_window [app_id="yuzu"] {
    focus
    move to output $vmon1

}
for_window [title="Game Installed on HDD"] {
   focus
}

for_window [app_id="pycalc"] {
    focus
    floating enable
    resize set 800 600
}

for_window [title="Microsoft Teams Notification"] floating enable
no_focus [title="Microsoft Teams Notification"]
for_window [class="Godot"] floating enable
for_window [title=".* - Godot Engine"] floating disable
for_window [app_id="Thunar" title="Rename.*"] floating enable


# Load Profile Sync Daemon (psd) before starting firefox
#exec --no-startup-id systemctl --user start psd.service

# Load layouts
#exec --no-startup-id "i3-msg 'workspace 1; append_layout ~/.config/i3/layout_tree/workspaces-1.json'"
#exec --no-startup-id "$term"
#exec --no-startup-id "sleep 0.2; i3-msg 'workspace 20; append_layout ~/.config/i3/layout_tree/workspaces-2-firefox.json'"
#exec --no-startup-id "sleep 2; firefox-developer-edition"


# Language Keyboard Layout Toggle
# TODO: set in wayland way
bindsym $mod+Ctrl+k exec --no-startup-id ~/.config/i3/togglekbmap.sh

#exec --no-startup-id wl-paste --watch cliphist store
#exec --no-startup-id wl-paste --primary --watch wl-copy
#exec --no-startup-id wl-clip-persist --clipboard both
#exec --no-startup-id clipboard-sync

# Load layouts in the sway way
input type:keyboard {
        xkb_layout us,it
        xkb_options grp:win_space_toggle
}

# Theme 

## Colours
set $dark        #3C3836
set $gray        #504945
set $lightGray   #DFDFDF
set $red         #EB4D4B
set $lightRed    #DD0000
set $orange      #D65D0E
set $darkYellow  #FFA500
set $yellow      #FABD2F
set $beige       #EBDBB2
set $light       #FBF1C7
set $white       #FFFFFF


## Window Colours
#client.<class>             <border>    <background>    <text>      [<indicator>    [<child_border>]]
client.focused               $light      $dark           $light      $orange         $light
client.focused_inactive      $dark       $dark           $light      $orange         $dark
client.unfocused             $dark       $dark           $light      $orange         $dark
client.urgent                $red        $red            $light      $orange         $dark
client.background            $dark       $dark           $dark       $dark           $dark
client.placeholder           $dark       $dark           $dark       $dark           $dark

seat seat0 xcursor_theme Oxygen_White 24

include /etc/sway/config.d/*
exec $HOME/.config/i3/sway_scripts/90-systemd-user.sh

#exec wayvnc -S /tmp/dp_vnc -f 30 -o DP-1 192.168.1.100 5900
#exec wayvnc -S /tmp/hdmi_vnc_f1 -f 1 -o HDMI-A-1 100.64.0.1 5901
#exec wayvnc -S /tmp/headless_vnc_f1 -f 1 -o HEADLESS-1 192.168.1.100 5902
